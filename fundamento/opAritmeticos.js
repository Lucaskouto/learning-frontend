const [a, b, c, d] = [2, 3, 20, 5];

const soma = a + b
const subtracao = b - a
const divisao = c / a
const multiplicacao = d * a
const modular = c % 4

console.log(soma, subtracao, divisao, multiplicacao, modular);

// Operadores binarios -> a + b, b - a...
// Operadores  unários -> -a +b
