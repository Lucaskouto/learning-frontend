const [a, b, c, d] = [2, 3, 20, 5];

const soma = a + b
const subtracao = b - a
const divisao = c / a
const multiplicacao = d * a
const modular = c % 2
const exponencial = b ** a

console.log(soma, subtracao, divisao, multiplicacao, modular, exponencial);

// Operadores binarios -> a + b, b - a...
// Operadores  unários -> -a +b
